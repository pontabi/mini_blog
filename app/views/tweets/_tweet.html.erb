<div id="<%= dom_id tweet %>" class="tweet-container">

  <%= link_to user_path(tweet.user), class: "tweet-user-link" do %>
    <div class="tweet-user-icon">
      <%= image_tag 'user_icon.png', alt: 'User Icon', class: 'icon-placeholder' %>
    </div>
    <%= tweet.user.username %>
  <% end %>
  <p class="tweet-content"><%= tweet.content %></p>
  <p class="tweet-timestamp"><%= l tweet.created_at %></p>
  <div class="tweet-actions">

    <% if tweet_owner?(tweet) %>
      <p class="tweet-edit-link"><%= link_to "Edit", edit_tweet_path(tweet) %></p>
      <p class="tweet-delete-button"><%= link_to "Delete", tweet, data: { turbo_method: :delete }  %></p>
    <% end %>
  </div>
</div>
